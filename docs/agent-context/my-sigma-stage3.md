# My Sigma·포트폴리오·가격/σ 이력 (2026-09-22)

## 계산 정의

- σ 산출의 유일한 출처는 oi_shock/sigma_core.py. 웹은 이미 발행된 anchor/sigmaPercent로 기존 lib/sigma.ts의 위치 계산과 상태 판정을 재사용한다.
- 관심목록 요약은 동일가중 mean(z), mean(abs(z)), 기존 상태 판정에 따른 ±1σ 경계 밖 개수, 유효/전체 개수다. 정확한 ±1도 경계 밖에 포함한다. 표시용 반올림을 판정에 사용하지 않는다.
- 경계 근접은 기존 보드의 0.85 ≤ |z| < 1을 상수로 통합했다. 새로운 신호 임계값이 아니다.
- snapshotId, 밴드 창, 종가 세션, methodVersion, priceBasis가 같아야 한다. 앵커 IV임을 확인할 수 없으면 평균을 숨긴다. 주중 IV를 과거 앵커 IV처럼 취급하면 선행 정보가 섞인다.
- 2026-09-21 자료 123종목 중 97개가 fromAnchor=true, 26개가 false였다. 가격/σ 원본은 바꾸지 않고 기준 메타데이터만 덧붙였다.
- 없는 종목/σ는 null. 분모는 유효 개수, 전체 저장 개수는 유지한다. +2와 −2의 평균은 0, 평균 절대 위치는 2다.

## 포트폴리오

`lib/portfolio.ts`가 계산 단일 출처다. 두 기능은 구별한다.

1. 비중 시뮬레이션: 기간 시작 비중으로 매수 후 보유, 리밸런싱 없음, 현금 이자 0. V(t)=cashWeight/100 + Σ weight_i/100 × price_i(t)/price_i(start). 기간 수익=(V(end)−1)×100. 일간 수익=(V(end)/V(prev)−1)×100. 기여도는 %p이며 합이 기간 수익과 같다. 기말 비중은 가격에 따라 달라진다.
2. 보유 관리: 수량·평단·보유 기준일. 현재 평가금액 입력 시 현재 종가로 수량을 한 번 환산해 고정한다. 평가손익=수량×(현재가−평단). 일간 손익은 이전 세션부터 동일 수량을 보유했을 때만 제공하며 전일 평가액이 분모다.

실제 계좌의 현금흐름/TWR/IRR, 매매 원장, 배당·FX·수수료·기업행동 자동 처리는 구현 범위에 없다. 화면에도 명시한다. 보유정보 변경은 사용자가 직접 입력한다.

공통 가격이 없는 종목은 재가중하지 않는다. 요청 기간 자료가 부족하면 기간을 몰래 줄이지 않는다. SPY 거래일이 확보돼 있으면 공동 누락일도 검사한다. 현재는 원본의 20거래일만 초기 이력으로 확보했다. 다음 발행부터 이미 조회하는 OHLC(최대 1000봉)를 별도 이력에 누적한다.

## 브라우저 저장

- 기존 관심목록 키/한도 20은 그대로 유지.
- `sigma-personal-portfolios:v1`: allocations, cashWeight, holdings, revision. 이전 정상 문서는 `:backup`에 백업하고 저장한다. JSON 내보내기/검사 후 명시적 가져오기 제공. 손상 자료를 자동 삭제하지 않는다.
- `sigma-watchlist-observations:v1`: 최대 4관측, snapshot/date/band/basis와 symbol별 z만 보관. 차트/GEX/계좌정보는 넣지 않는다.
- 같은 ID는 비교하지 않으며 같은 세션 정정도 새 경계 통과로 세지 않는다. 새 주간 창은 별도 이벤트. 새로 추가한 종목은 데이터 복구로 세지 않는다.
- 다른 탭 저장이 감지되면 입력 초안을 유지하고 덮어쓰기를 거부한다. 초안 내보내기/저장본 불러오기로 해결한다.
- 보유정보·평단·금액·전체 티커 배열을 분석 이벤트에 넣지 않는다. 서버에 포트폴리오를 저장하지 않는다.

## 가격/σ 차트

- Recharts SVG. 데스크톱은 가격(왼쪽 $)과 σ(오른쪽)를 한 차트로 비교한다. 모바일은 가격+밴드 / σ 위치를 전환한다. σ는 주마다 선을 끊어 밴드 재설정을 가격 복귀처럼 그리지 않는다.
- 금요일 종가는 끝난 주의 밴드에 속한다. 새 앵커의 0σ로 덮어쓰지 않는다.
- fromAnchor=true인 과거 밴드만 σ/이탈 후 표본에 사용한다. 기준을 확인하지 못한 구간은 가격만 표시한다.
- 완료 주의 ≥+1σ / ≤−1σ 필터, 1·2·4주 후 가격수익, 선택 마감가를 0%로 놓은 후속 가격 경로. 단순 관찰이며 예측력은 **미검증**. 아직 도래하지 않은 기간은 —.
- 공개 이력은 `/snapshot/history/SYMBOL.json`. 서버가 현재 스냅샷과 병합하며 오류 시 현재 확보된 이력만 표시한다. 없는 과거 밴드를 현재 IV로 복원하지 않는다.

## 공유·SEO

- 공유 카드 1080×1350, OG 1200×630. 공통 symbolCardData → Next ImageResponse. 화면 캡처/차트 캡처가 아니다. 폰트는 라이선스와 함께 저장했다.
- 이미지 준비와 공유를 별도 사용자 클릭으로 분리. files canShare 확인, 미지원/실패는 다운로드, AbortError는 취소로 처리.
- 이미지 URL은 실제 snapshotId를 포함한다. 원본 정정 후에도 같은 숫자를 쓰도록 공개 축약 revision을 90일 제공한다. 만료된 이미지는 410이며 최신 수치로 몰래 대체하지 않는다. 종목 페이지 링크 자체는 최신 페이지다.
- invalid ticker는 스트리밍 시작 전 proxy.ts에서 실제 HTTP 404. valid symbol 메타데이터/HTML의 언어와 숫자가 같은 loadBoard를 사용한다. canonical/sitemap 및 기존 force-dynamic 정책은 보존.
- 관련 종목은 현재 자료의 같은 분류 섹터, 겹치는 테마 수, 티커순으로 최대 6개. 무작위 선정 없음.

## 운영

실제 데이터 소스는 Blob이 아니라 `https://sigma-snapshot-data.vercel.app/snapshot/latest.json`이다. oi_shock의 기존 발행 잡이 이력도 함께 배포한다. 주간 σ 캐시 KEEP_WEEKS, launchd 시간, 알람 채널, dedup/schedule 상태는 수정하지 않는다.

배포 전 점검 기록: `docs/reviews/2026-09-22/stage3-preflight.md`.
검증/실기기 점검 목록: `docs/reviews/2026-09-22/stage3-validation.md`.

## 2026-09-23 가격·σ 이중 축 차트

- `PriceSigmaOverlayChart`는 상세 이력 차트만 교체한다. 상단 30분봉/GEX 차트와 대문 차트는 유지한다.
- `historyChartRows`가 날짜별 `close/anchorClose/upper1Sigma/lower1Sigma/sigmaPosition`을 제공한다. 기존 `calculateZScore`·`sigmaPriceRange`를 재사용하며 현재 IV로 과거 밴드를 복원하지 않는다. 금요일 귀속·fromAnchor 가드 유지, 누락은 null이다.
- 종가 청록색, 상단 보라색, 하단 노란색, σ는 테마 전경색. 밴드 경계와 [lower, upper] range Area는 stepAfter, 종가는 monotone. σ 선은 주별로 분리한다.
- `priceSigmaDomains`가 선택 기간의 가격·밴드·σ만으로 두 축을 갱신한다. σ축은 항상 −1/0/+1을 포함하며 실제 이탈값을 자르지 않는다.
- 공개 MarketHistory에는 일별 close만 있다. 마커는 매 거래일의 종가 기준 경계 도달·돌파(등호 포함)이며 장중 터치가 아니다. 장중 high/low를 추가 수집하거나 추정하지 않는다.
- 툴팁은 종가·상하단·앵커·σ를 함께 표시하며 없는 값은 —. 모바일(<640px)은 가격+밴드 기본, σ 위치 전환. 외부 요청 추가 없음.
- 로컬 검증: 전체 테스트 92개 통과(기존 89+신규 3), 변경 파일 ESLint/TypeScript, webpack 프로덕션 빌드 통과. 첫 빌드의 Fonts DNS 제한은 네트워크 허용 후 재실행해 해결했다.
- 실제 공개 AMD 2026-09-22 종가 $623.77, +1σ $596.54, −1σ $523.10, 앵커 $559.82, +1.74σ가 요약·툴팁과 일치. 제시 예시의 반올림된 밴드 너비 36.72 기준 단위검사 값은 +1.7415577342σ.
- 브라우저 검증: 데스크톱 1270px, 모바일 viewport 390/360/430px에서 가로 넘침 없음(모바일 clientWidth/scrollWidth 각각 380/380, 350/350, 420/420; 스크롤바 제외). 1M/3M/1Y 축 변화·모바일 두 모드·한영·툴팁 확인, 콘솔 오류/경고 0.
- 최초 차트 구현 당시에는 로컬만 검증했다. 후속 사용자 요청으로 My Sigma 개선과 함께 운영 배포한다(아래 배포 기록 참고).

## 2026-09-23 My Sigma 요약 간소화·전체 필터·GEX 근접

- 사용자 요청: 요약 UI의 평균 절대 위치/분석 가능 삭제. 평균 위치와 ±1σ 경계 밖 두 개만 유지한다. 계산 모델의 검증용 필드와 누락/동일기준 가드는 유지한다.
- 버그 원인: 요약이 `attentionStocks(selected)`의 기본 limit=3을 항상 사용했고 필터를 읽지 않았다. 상세 My Sigma 요약은 `summarySymbolRows`로 저장한 모든 티커를 표시하고(누락 종목도 저장 유지 행), 아래 카드와 같은 `matchesSigmaFilter`를 사용한다. 전체는 관심목록 전체이며 별도 포트폴리오 보유·비중 자료를 몰래 합치지 않는다. 홈 compact는 3개 미리보기와 전체 N개 링크를 유지한다.
- 필터를 목록 위로 옮기고 전체/경계 밖/경계 근접에 건수를 표시한다. 행은 티커+종가 / 색상 σ+상태 / GEX 문맥으로 나눈다. 설명은 표시 기준 접기 영역에 둔다.
- `lib/gex-proximity.ts`가 GEX 근접 표시의 단일 출처. 기존 `selectGexChartLevels`의 S1/S2·R1/R2 양수 후보 중 종가 ±1% 이내를 사용하고 방향별 가장 가까운 후보 하나씩 표시한다. 기존 강도/합치/σ 기준은 바꾸지 않는다. 1%는 새 UI의 거리 기준이며 예측력 기준이 아니다. 지지·저항 효과는 미검증으로 표시 기준에 명시한다.
- 종가/행사가 원값으로 판정한 뒤 가격·거리(%)를 반올림한다. 넘어간 저항을 지지로 재분류하지 않는다. 자료 없음/근접 없음/OI 기준일 다름을 구분하고 세션과 OI 날짜가 다르면 근접 판정하지 않는다. 새로운 API 호출 없음.
- 검증: 전체 95개 테스트(직전 92+추가 3), TypeScript/변경 파일 ESLint/webpack 빌드 통과. 샘플 6개 티커로 전체6→경계 밖4→전체6, 빈 근접 필터 확인. 사용자 브라우저 저장 자료는 변경하지 않았다.
- 실제 2026-09-22 공개 자료: SOXX 종가572.78/지지570(−0.49%), MU1096.16/저항1100(+0.35%), NVDA228.87/저항230(+0.49%). 이는 가격 거리이며 효과 검증 결과가 아니다.
- 모바일 viewport360/390/430px 가로 넘침 없음. 브라우저 경고/오류 0.

### 운영 배포 완료

- 2026-09-23, 코드 커밋 `69650e9`. `vercel --prod --scope svpk1` 직접 배포.
- `dpl_9j81N85S4EymKHc1CUjDFPWt8KuB`, READY/production. 배포 URL https://sigma-dashboard-fo4rljuxx-svpk1.vercel.app → https://sigma-dashboard-five.vercel.app 별칭 연결.
- 운영 My Sigma의 샘플 전체6/경계 밖4/전체6 전환과 GEX 가격·거리 표시 확인. 평균 절대 위치/분석 가능 항목 없음. 390px에서 clientWidth=scrollWidth=380, 콘솔 오류0.
- 운영 AMD 상세에서 좌측 가격·우측 σ축, 종가623.77/상단596.54/위치+1.74σ, 범례·음영을 확인했다.
- 사용자 저장 목록·보유정보와 발행 스케줄/데이터 산식은 수정하지 않았다.


## 2026-09-23: 1년 추세와 작은 정보 표시줄

- 상세 차트 기본값을 3M에서 1Y로 변경. 1M/3M/1Y는 4/13/52주 근사 대신 달력 월 단위로 계산하며 월말/윤년은 유효한 말일로 보정한다. ALL은 보유 이력 전부.
- 가격과 밴드가 보이는 범위, σ를 계산할 수 있는 거래일 수를 별도로 표시한다. 없는 σ는 null 유지.
- 차트 위에 뜨던 큰 툴팁 및 중복 요약을 없애고, Recharts Tooltip portal을 차트 밖 고정 영역에 연결했다. 데스크톱 한 줄/모바일 두 줄에 날짜·종가·±1σ 가격·σ 위치를 표시한다. 포인터와 방향키로 날짜 선택 가능. 초기값은 최신 날짜.
- 장기 X축은 월별 눈금으로 연도를 구분하고 같은 월의 반복 라벨을 없앴다. 오른쪽 σ축은 정수 경계로 표시. 기간 전환 때 좌우 축 범위도 다시 계산한다.
- 사용자가 σ 위치의 분리 여부는 1년 추세를 본 뒤 결정하겠다고 했으므로, 데스크톱의 우측 축 및 모바일 가격/σ 전환 방식은 유지했다.
- 과거 밴드는 oi_shock의 새 `backfill_dashboard_history.py`로 앵커일 UW term structure를 조회해 복원한다. 공용 sigma_core의 기존 선택/환산식을 사용하고 기존 저장 밴드는 덮어쓰지 않는다. 이후 일상 발행은 별도 추가 호출 없이 이력을 유지한다.
- 로컬 AMD: 1Y 2025-09-22~2026-09-22 252거래일, 밴드252/252. 1M21거래일, 3M65거래일, ALL270거래일(밴드266/270). 390px 모바일에서 가로 넘침 없고 큰 툴팁이 차트를 가리지 않는 것을 확인했다.
- 검증: 웹96개, upstream40개 테스트 통과. ESLint/TypeScript/Next 운영 빌드 통과. 브라우저 오류 로그0개.


### 1년 차트 운영 반영 완료

- 홈페이지 코드 `edeaa7a`, 배포 `dpl_5gXYEhW8GEi2XjNGyfSszqoMerfz`, READY/production. https://sigma-dashboard-l44cjgvti-svpk1.vercel.app → https://sigma-dashboard-five.vercel.app
- 데이터 배포 https://sigma-snapshot-data-pqfcdcl9z-svpk1.vercel.app → 기존 sigma-snapshot-data.vercel.app. 최신 스냅샷/달력/revision/API는 원본 그대로, 123종목 과거 밴드6,255개 추가. 기존가격29,907개·기존밴드340개 전수 보존.
- 최근1년 가격116종목252거래일, 7종목은 확보된 기간만 표시. 전체 σ지원29,525/29,907거래일(98.72%), 81종목252/252. 기존 fromAnchor=false와 과거 입력 누락은 null 유지.
- AMD/NVDA/SOXX/MU/EWY는252/252. 대표7종목 공개 JSON과 로컬 원본이 전부 일치했다. 운영 AMD 페이지에서 기본1Y 및 `σ 밴드 252/252거래일` 직접 확인.
- 서버 history fetch는 revalidate300이므로 배포 직후 첫 요청은 이전 이력을 반환하며 백그라운드 갱신할 수 있다. 첫 요청11/252 → 다음 새로고침252/252를 확인했다. 캐시 우회 URL로 결과를 숨기지 않았다.
- σ 위치를 독립 패널로 분리할지는 사용자가 1년 추세를 본 뒤 정한다.


## 2026-09-23: 전체 종목 첫 조회 캐시 문제와 σ 하단 분리

- 사용자 제보: AMD 외 종목이 잘 안 보임. 최초 주요10종목 확인만으로는 놓쳤으나 **운영123종목 첫 응답 전수대조에서90종목의 밴드 커버리지가 원본보다 짧음**을 확인했다. ALAB/AVGO/GOOGL/QQQ/SPY 등은11/252, 일부는0이었다. 앞선 배포의7개JSON/AMD화면 검증은 전체 상세 페이지의 첫조회 캐시를 검증하지 못했다.
- 원인: 같은 고정 history URL을 `next.revalidate=300`으로 읽는 종목별 오래된 응답. 자료 복원 후에도 첫 요청이 구 이력을 반환하고 다음 요청에서 갱신했다. 가격은 이미1년치여서 가격일수만 확인하면 놓친다.
- `loadMarketHistory`를 기존 최신스냅샷과 동일한 `cache:no-store`로 변경. 정적 데이터 배포를 읽으며 UW호출은 없다. 8초 timeout, 실패1회재시도, 모두 실패하면 최근자료 표시와 재시도 버튼을 명시. 실패를 정상적인 짧은 이력으로 숨기지 않는다.
- 가격+밴드는 상단, σ위치는 하단 독립패널. 모바일도 동일하며 기존 모바일 모드 전환은 제거했다. 두 패널의 X축/여백/축너비를 일치시키고 날짜기준으로 커서와 상단 숫자줄을 동기화한다. 주간 σ리셋 간 선연결은 금지한다.
- 1Y기본값과 작은 고정 정보줄 유지. 가격패널에는 가격단위만, 하단에는σ단위만 표시한다.
- 테스트100개 통과(새 캐시/재시도/잘못된 종목/명시적 실패상태4개). TypeScript/ESLint/Next운영빌드 통과. MU데스크톱·390px모바일에서 상하분리, 날짜동기화, 기간전환, 가로넘침없음, 콘솔오류0 확인.
- 전수대조 원본: `/private/tmp/sigma-all-pages-before.json`, 불일치90개 `/private/tmp/sigma-history-stale-before.json`. 후속검증 도구는 작업폴더 `audit-history-pages.py`; 실제서버HTML의 가격일수와밴드커버리지, 두패널존재를 종목별원본과 비교한다.


### 캐시 수정·분리 차트 배포 검증

- 코드 `d2fca8a`, 운영 배포 `dpl_5iBgMJU16U6NAaV9Hsj3B7KFHFPX` READY. https://sigma-dashboard-cbt8z6l84-svpk1.vercel.app → sigma-dashboard-five.vercel.app.
- 운영123개 상세 페이지를 각1회 조회해 공개HTML의 날짜범위·가격일수·밴드일수·가격/σ독립패널 존재를 로컬원본과 대조: **123/123통과, 불일치0(수정전90)**. 결과 `/private/tmp/sigma-all-pages-after.json`. 동일 검사에서 데이터가 짧은7종목과 fromAnchor=false/과거자료누락 구간도 실제보유범위대로 일치함을 확인했다.
- ALAB 운영 브라우저: 이전11/252 →252/252, 상단가격+하단σ 확인. 모바일390px에서도 두패널 유지, 가로넘침없음. MU하단패널 방향키가 상단숫자줄 날짜/값을 동기화함을 확인.
- 이전부터 열린 브라우저에는 구 화면이 남을 수 있으므로 배포후1회새로고침 안내. 서버의 종목별 stale응답을 유지하고 사용자에게 여러번새로고침하게 하는 방식으로 되돌리지 말 것.


## 사용자 표시 규칙 변경: σ 위치 곡선 연결

- 사용자가 주간별로 끊긴 σ 선이 불편하다며 부드럽게 이어 달라고 명시했다. 위의 과거 기록에 있는 주간 경계 연결 금지는 이 요청으로 폐기한다.
- 하단 패널은 날짜별 sigmaPosition 하나의 시리즈를 monotone 곡선으로 연결한다. 주가/σ 계산값과 툴팁은 그대로이며 평균화하거나 재계산하지 않는다.
- 실제 σ 자료가 없는 null 구간은 connectNulls=false로 유지한다. 가격/밴드 패널과 날짜 동기화도 유지한다.
