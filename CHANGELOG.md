# 패치노트

## v0.0.1 — 2026-08-19

첫 배포 이후 대시보드를 실제로 보면서 걸렸던 것들을 정리한 판입니다.
σ 산식은 손대지 않았습니다. 계산은 여전히 `oi_shock/sigma_core.py` 한 곳에서만
나오고, 이번 변경은 전부 그 숫자를 **어떻게 보여주는가**에 대한 것입니다.

### 밴드 눈금에서 바로 가격 읽기

상세 패널의 "Position in band" 축 라벨(−1.5σ / −1σ / Anchor / +1σ / +1.5σ)에
커서를 올리거나 손가락을 대면 이름이 그 자리에서 **금액으로 바뀝니다.**
전에는 "+1σ가 얼마인가"를 알려면 아래 표까지 내려가 눈으로 맞춰야 했습니다.

라벨과 가격이 같은 그리드 칸을 쓰기 때문에 글자가 바뀌어도 옆 눈금은 밀리지
않습니다. 모바일 사파리가 버튼 탭에 포커스를 주지 않는 경우가 있어 `active`
상태도 같이 물려 뒀습니다 — 손가락을 대고 있는 동안에는 어떤 브라우저에서든
숫자가 보입니다.

### 축을 밴드 끝(±2σ)까지

메인 그리드의 스트립 플롯이 ±1.5σ에서 잘려 있었습니다. 정작 그 바깥이 알람이
울리는 구간인데 눈금이 없으니 얼마나 벗어났는지 가늠이 안 됐습니다. 축을
±2σ까지 늘려 `−2σ / −1.5σ / −1σ / Anchor / +1σ / +1.5σ / +2σ` 전부 표시합니다.

### 하루치 주가 선차트

상세 패널의 가격 차트가 한 달 종가 경로였는데, 이제 **당일 정규장(09:30–16:00,
5분봉)** 을 그립니다. 앵커와 ±1σ는 그대로 겹쳐 놓았습니다.

다만 y축은 밴드 전체를 억지로 끼워 넣지 않습니다. 주간 σ는 하루 변동폭의 몇
배라, 밴드를 다 담으면 그날 움직임이 직선으로 눌려버립니다(측정해 보니 중앙값
1.35배, 최대 5.27배 압축). 그래서 **축은 그날을 따라가고**, 축 밖으로 나간
레벨은 위/아래 가장자리에 화살표와 함께 이름과 가격으로 적어 둡니다. 조용히
빼버리면 "밴드 근처였다"는 반대 인상을 주기 때문입니다.

옵션 피드가 비어 있는 종목은 기존 한 달 경로 차트로 자동 폴백합니다.

### 섹터 트리맵

타일에 **주가를 같이 적고**, 섹터 이름 글자를 키웠습니다. 잘 안 보인다는
지적 그대로입니다.

### 극단 리스트 제목에 색

첫 화면 좌측 "Most overheated / Most oversold"가 무채색이라 눈에 안 띄었습니다.
각자 향한 밴드 끝의 색을 입혔습니다.

### 유니버스 확대 (+16종목)

| 그룹 | 추가 |
|---|---|
| 지수 ETF | SCHD, DIA, GLD, SLV |
| 경기방어 | WMT, PEP, KO, COST |
| 전력 | ETN, VRT, BE, GEV |
| 네오클라우드 | KEEL, WULF |
| 반도체 | TER |
| 광통신 | SMTC |

현재 **75종목 + 섹터 ETF 11개**, 전 종목에 인트라데이와 GEX가 모두 붙습니다.

### 배관 정리 (눈에 안 보이는 것)

- 발행 잡의 출력 경로를 `oi_shock/state/` 로 옮겼습니다. 예전엔 웹앱의
  `data/snapshot.json`(커밋된 개발용 시드)에 직접 써서, 매일 아침 07:00에
  아무도 커밋할 생각 없는 diff가 웹앱 저장소에 새로 생겼습니다.
- 업로드 전에 **커버리지 검사**를 겁니다. 생성기는 종목별 실패를 모아두고 남은
  것으로 멀쩡한 JSON을 끝까지 쓰기 때문에, UW가 흔들린 날 "75종목 중 4종목"
  스냅샷이 조용히 올라가 보드가 텅 빌 수 있었습니다. 이제 미달이면 업로드를
  중단하고 웹앱은 직전 데이터를 계속 봅니다.
- 접근성: 위치 바의 `role="img"`를 트랙에만 남겼습니다. 축 라벨이 포커스 가능해
  졌는데, 스크린리더는 `img` 안의 내용을 라벨 하나로 뭉개버려 그대로 두면 새
  기능이 통째로 가려집니다.

### 그대로인 것

- σ 산식 (`anchor × sigmaPercent / 100`, `z = (price − anchor) / σ`)
- 임계값 ±1.5σ, 종가 기준 하루 1회 갱신
- 스냅샷 `schemaVersion: 1` — 필드 추가는 버전을 올리지 않습니다
- Blob 실패 시 폴백 없음. 시드로 되돌아가지 않고 데이터 없음 상태를 띄웁니다
